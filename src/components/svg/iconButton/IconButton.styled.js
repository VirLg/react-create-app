import styled from '@emotion/styled';
export const InonBtn = styled.button`
border-radius: 50%;
border: none;
background-color: inherit;
 cursor: pointer;
 &:hover,
 &focus{
    transform: scale(1.3);
 }
}`;
